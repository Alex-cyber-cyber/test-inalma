
# Frontend (React)

- Vite + React 18 + Vitest + Testing Library.
- Variables:
  - `VITE_API_URL` (opcional, default `http://localhost:8000/api`).

## Comandos
```
npm install
npm test
npm run dev
```
